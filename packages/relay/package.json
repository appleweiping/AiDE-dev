{
  "name": "@aide/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AiDE relay server — bridges desktop daemon and mobile app",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "build": "tsdown src/index.ts --outDir dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@aide/shared": "workspace:*",
    "tsdown": "^0.12.0",
    "typescript": "^5.4.5"
  }
}
