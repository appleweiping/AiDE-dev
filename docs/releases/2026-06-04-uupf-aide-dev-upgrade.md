# 2026-06-04 UUPF AIDE_DEV Upgrade

This repository was reviewed with Universal Upgrade Forge from
`D:\AGENTIC_SCIENCE\uupf\UniversalUpgradeForge.zip`.

Local audit run:

- `D:\AGENTIC_SCIENCE\.work\uupf_runs\aide-dev`
- 108 iteration records were generated from a Git archive snapshot to avoid
  copying `node_modules`, local secrets, or cache directories.
- UUPF produced an offline upgrade plan; public repository changes were
  materialized manually.

Materialized changes:

- Promoted the public project identity from `AiDE-dev` to `AIDE_DEV`.
- Updated the README release URL and project title.
- Added this traceable upgrade note.

Safety boundary:

- Keep provider keys, local MCP environment variables, user config, logs,
  sessions, and cache directories out of public commits.
